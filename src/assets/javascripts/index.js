const { borderCSS } = require('./border')
const { colorCSS } = require('./color')
const { display } = require('./display')
const { flex } = require('./flex')
const { headingCSS } = require('./heading')
const { marginCSS } = require('./margin')
const { paddingCSS } = require('./padding')
const { paletteCSS } = require('./palette')
const { roundedCSS } = require('./rounded')
const { sizes } = require('./sizes')
const { textCSS } = require('./text')

const style = {
  borderCSS,
  colorCSS,
  display,
  flex,
  headingCSS,
  marginCSS,
  paddingCSS,
  paletteCSS,
  roundedCSS,
  sizes,
  textCSS
}

module.exports = style
