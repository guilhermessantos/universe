import styled from 'styled-components'

export const Container = styled.div`
  bottom: 0;
  position: absolute;
  opacity: 0;

  svg {
    display: inline-block;
    position: absolute;
    height: 10px;
    width: 10px;
  }
`
