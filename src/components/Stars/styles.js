import styled from 'styled-components'

export const Container = styled.div`
  position: absolute;
  bottom: 150px;

  svg {
    display: inline-block;
    position: absolute;
    height: 10px;
    width: 10px;
  }
`
